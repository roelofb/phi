import type { Sandbox, SandboxOptions } from "./types.js";

export async function createDaytonaSandbox(
  _opts: SandboxOptions,
): Promise<Sandbox> {
  throw new Error("Daytona sandbox not yet implemented");
}
